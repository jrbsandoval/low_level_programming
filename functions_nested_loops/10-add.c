#include "main.h"

/**
 * add- Function that adds two integers and returns the result
 * @a: First parameter type integer
 * @b: Second parameter type integer
 * Return: add two parameters
 */

int add(int a, int b)
{
	return (a + b);
}
